{
  "$schema": "https://json-schema.org/schema",
  "version": 2,
  "title": "GenerateTypes executor",
  "description": "",
  "type": "object",
  "properties": {
    "envFile": {
      "type": "string",
      "description": "Path to the .env file"
    },
    "output": {
      "type": "string",
      "description": "Path to the output file"
    }
  },
  "required": ["envFile", "output"]
}
